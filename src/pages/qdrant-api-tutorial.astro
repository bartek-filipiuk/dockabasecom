---
import DocLayout from '../layouts/DocLayout.astro';
import { Content as TutorialContent } from '../../docs/materialy/Qdrant API Tutorial.md';
---

<DocLayout title="Qdrant API Tutorial">
  <div class="tutorial-content">
    <TutorialContent />
  </div>

  <style>
    .tutorial-content {
      max-width: 900px;
      margin: 0 auto;
      padding: 1rem;
    }
    .tutorial-content :global(h1) {
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 1.5rem;
    }
    .tutorial-content :global(h2) {
      font-size: 1.5rem;
      font-weight: bold;
      margin-top: 2rem;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid var(--color-border);
    }
    .tutorial-content :global(h3) {
      font-size: 1.25rem;
      font-weight: bold;
      margin-top: 1.5rem;
      margin-bottom: 0.5rem;
    }
    .tutorial-content :global(p) {
      margin-bottom: 1rem;
      line-height: 1.6;
    }
    .tutorial-content :global(ul), .tutorial-content :global(ol) {
      margin-left: 1.5rem;
      margin-bottom: 1rem;
    }
    .tutorial-content :global(li) {
      margin-bottom: 0.5rem;
    }
    .tutorial-content :global(pre) {
      background-color: var(--color-bg-code);
      padding: 1rem;
      border-radius: 0.375rem;
      margin: 1rem 0;
      overflow-x: auto;
    }
    .tutorial-content :global(code) {
      font-family: monospace;
    }
    .tutorial-content :global(blockquote) {
      border-left: 4px solid var(--color-accent);
      padding-left: 1rem;
      margin: 1rem 0;
      font-style: italic;
    }
  </style>
</DocLayout>
